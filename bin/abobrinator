#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require_relative "../lib/abobrinator"

begin
  Abobrinator::CLI.start(ARGV)
rescue Interrupt
  puts "\nInterrompido pelo mestre. O abobrinator parou."
  exit 1
end
